create database systemCadastroLogin;
use systemCadastroLogin;

create table User (
    ID int Primary Key auto_increment,
    nameUser Varchar(30),
    email Varchar(50),
    senha Varchar(256)
    typeUser 
)
